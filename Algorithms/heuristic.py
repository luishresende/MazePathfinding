def heuristic(node, objective):
    """
    Calcula a heuristica entre um nó e o objetivo

    Args:
        node (str): O nó atual.
        objective (str): O nó objetivo.

    Returns:
        float: A distância heuristica.
    """

    distance = {}
    return distance.get(node, float('inf'))